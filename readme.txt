1. PYTHON server:
    uvicorn main:app --host 192.168.31.100 (IP adresa přidělena počítači)
2. ARDUINO přepsat adresu podle výše uvedené
3. zapnout postgres (local)
4. změnit IP adresu serveru na výše uvedenou (na FE) v .env